# ===========================================
# Test Environment Configuration
# ===========================================

ENVIRONMENT=test

# Test database connection (set via docker-compose using GAME_NAME)
# POSTGRES__DSN is configured in docker-compose.yml environment

# Analytics disabled for tests
ANALYTICS_ENABLED=false

# Test secrets
TG_BOT_TOKEN=test_token
SECRET_KEY__JWT=test_secret_key_for_testing_only
